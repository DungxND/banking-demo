apiVersion: v2
name: banking-demo
description: Banking Demo Phase 8 — Kong → API Producer → RabbitMQ → Consumers
type: application
version: 0.1.0
appVersion: "8"
keywords:
  - banking
  - microservices
  - kong
  - rabbitmq
  - redis
# Helm 4 defaults to Server-Side Apply (SSA) on fresh installs; SSA is GA since k8s 1.25.
kubeVersion: ">=1.25"
