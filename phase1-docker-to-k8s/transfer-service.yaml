# Phase 1: Transfer Service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfer-service
  namespace: banking
  labels:
    app: transfer-service
    app.kubernetes.io/name: transfer-service
    app.kubernetes.io/part-of: banking-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transfer-service
  template:
    metadata:
      labels:
        app: transfer-service
        app.kubernetes.io/name: transfer-service
        app.kubernetes.io/part-of: banking-demo
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        runAsNonRoot: true
        runAsUser: 1000
      imagePullSecrets:
        - name: github-registry
      containers:
        - name: transfer-service
          image: ghcr.io/dungxnd/banking-demo/transfer-service:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8003
              name: http
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: banking-db-secret
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: banking-db-secret
                  key: REDIS_URL
            - name: CORS_ORIGINS
              value: "http://localhost:3000"
            - name: OTEL_SERVICE_NAME
              value: transfer-service
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: service.namespace=banking-demo,deployment.environment=production
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: grpc
            - name: NODE_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://$(NODE_IP):4317"
            - name: AUTOWRAPT_BOOTSTRAP
              value: instana
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: transfer-service
  namespace: banking
  labels:
    app: transfer-service
    app.kubernetes.io/name: transfer-service
    app.kubernetes.io/part-of: banking-demo
spec:
  selector:
    app: transfer-service
  ports:
    - port: 8003
      targetPort: 8003
      name: http
